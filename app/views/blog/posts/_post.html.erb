<div id="<%= dom_id post %>">
  <p>
    <%= link_to post.title, post %>
    — by <%= post.user.name %>
    <% if post.topic.present? %>
      | <%= post.topic.titleize %>
    <% end %>
  </p>
</div>
